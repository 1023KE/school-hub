
- Refined Data Fetching:
    - Added `classroom.courseworkmaterials.readonly` scope to Auth.js.
    - Set `pageSize: 100` for Classroom API calls (CourseWork, Announcements, Materials) to ensure no data is missed.
- Simplified Navigation:
    - Removed "Others" (その他) tab as per user request.
    - Added "All" (すべて) tab as the primary view, showing a unified feed of Classroom, Outlook, and Teams data.
- User Preference Enforcement:
    - In "All" (すべて) tab, expired items are displayed with full opacity (not grayed out).
    - Strengthened keyword matching for "Assignments" (課題) to include "フォーム", "アンケート", "振り返り", "評価", "コメント", "ワーク", and "宿題".
- Deployment Readiness:
    - Set Git identity to Name: "KE", Email: "1023KE".
    - Code is committed and ready for GitHub/Vercel.
    - Automated deployment is currently waiting for Browser Relay connection or manual `gh auth login`.

- Deployment Progress:
    - Attempted automated deployment via Browser Relay, but encountered connection issues.
    - Verified `gh auth status` shows user is not logged in.
    - Provided "magic line" `gh auth login --web` to help user authenticate via CLI.
- Final Categorization Strategy:
    - Keywords for `課題` (Assignments): "レポート", "課題", "宿題", "提出", "試験", "フォーム", "アンケート", "振り返り", "コメント", "評価", "ワーク".
    - Logic: Check title and first line of content for these keywords. Even if Classroom marks it as an "Announcement", if it has these keywords, it's a `課題`.
- Tab Behavior:
    - `すべて` (All): Shows everything from Classroom, Outlook, and Teams. No dimming for expired items.
    - `課題` (Assignments): Shows tasks. Expired items are dimmed and can be toggled.
    - `お知らせ` (Announcements): Shows general notices and integrated Outlook/Teams data.
- UI Design:
    - Fixed UI scale issue where it was too large; reverted to a slim, compact layout (max-width 1200px) to minimize scrolling.
- Technical Fixes:
    - Added `classroom.courseworkmaterials.readonly` scope to access "Materials" in Google Classroom.
    - Increased `pageSize` to 100 for all Classroom API list calls.
